{
    "name": "node-red-contrib-max-client",
    "version": "1.0.12",
    "description": "Sends a message to the messenger MAX via the MAX-bot",
    "main": "max-send.js",
    "repository": {
        "type": "git",
        "url": "https://github.com/teluse7/node-red-contrib-max-client"
    },
    "author": "teluse7",
    "scripts": {
        "test": "echo \"без тестов\" && exit 0"
    },
    "keywords": [
        "node-red",
        "MAX",
        "max",
        "messenger"
    ],
    "license": "Apache-2.0",
    "node-red": {
        "nodes": {
            "max-send": "max-send.js"
        }
    },
    "dependencies": {
        "puppeteer": "^17.0.0"
    }
}
