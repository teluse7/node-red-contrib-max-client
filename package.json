{
    "name": "node-red-contrib-max-client",
    "version": "1.0.9",
    "description": "Sends a message to the messenger MAX via the MAX-bot",
    "main": "max-send.js",
    "scripts": {
        "test": "echo \"без тестов\" && exit 0"
    },
    "keywords": [
        "node-red",
        "MAX",
        "max",
        "messenger"
    ],
    "license": "Apache-2.0",
    "node-red": {
        "nodes": {
            "max-send": "max-send.js"
        }
    },
    "dependencies": {
        "puppeteer": "^17.0.0"
    }
}
